
var Inspections = require('../models/inspections.js');

exports.average_criticals_per_restaurant = function(req, res) {
	/*var o = {};
	o.map = function () { emit(this.critical_violations.total,1) }
	o.reduce = function (k, vals) { return vals; }

	var q = Inspections.find({})
	.sort({'critical_violations.total': -1})
	.sort({'noncritical_violations.total': -1})
	.limit(20);
	q.exec(function (err, data) {
	  if (err) return handleError(err);
	  res.send(data);
	})*/
}